module Rumerical
  module LinearAlgebra
    def gaussj b
      
      indxc = Matrix.new({})
      indxr = Matrix.new({})
      ipiv = Matrix.new({})

      @inverse = Matrix.new({})
    end
  end
end
